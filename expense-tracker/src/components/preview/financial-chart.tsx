"use client";

import { Area, AreaChart, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

type TrendPoint = { month: string; revenue: number; expenses: number; profit: number };
export function FinancialChart({ data }: { data: TrendPoint[] }) {
  return <><div className="financial-chart" role="img" aria-label="Illustrative six-month revenue, expense and net profit trend. Exact values are available in the data table below."><ResponsiveContainer width="100%" height="100%" minWidth={0} minHeight={200}><AreaChart data={data} margin={{ top: 15, right: 12, left: 0, bottom: 5 }}><defs><linearGradient id="revenueFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#135cf7" stopOpacity={.16} /><stop offset="100%" stopColor="#135cf7" stopOpacity={0} /></linearGradient></defs><CartesianGrid vertical={false} stroke="#edf0f5" strokeDasharray="4 4" /><XAxis dataKey="month" axisLine={false} tickLine={false} tick={{ fill: "#7b8799", fontSize: 11 }} dy={8} /><YAxis tickFormatter={(value: number) => `${value / 1000000}m`} axisLine={false} tickLine={false} tick={{ fill: "#7b8799", fontSize: 11 }} width={42} /><Tooltip formatter={(value, name) => [`₦${Number(value).toLocaleString("en-NG")}`, name]} contentStyle={{ borderRadius: 12, border: "1px solid #e3e8f2", fontSize: 12 }} /><Legend iconType="circle" iconSize={7} wrapperStyle={{ fontSize: 11, paddingTop: 18 }} /><Area type="monotone" dataKey="revenue" name="Revenue" stroke="#135cf7" strokeWidth={2.5} fill="url(#revenueFill)" isAnimationActive={false} /><Area type="monotone" dataKey="expenses" name="Expenses" stroke="#e49a40" strokeWidth={2} fill="transparent" isAnimationActive={false} /><Area type="monotone" dataKey="profit" name="Net profit" stroke="#209681" strokeWidth={2} strokeDasharray="4 4" fill="transparent" isAnimationActive={false} /></AreaChart></ResponsiveContainer></div><details className="chart-data"><summary>View chart data</summary><div className="table-scroll"><table><caption>Illustrative trend amounts in NGN</caption><thead><tr><th>Month</th><th>Revenue</th><th>Expenses</th><th>Net profit</th></tr></thead><tbody>{data.map((row) => <tr key={row.month}><th>{row.month}</th><td>{row.revenue.toLocaleString()}</td><td>{row.expenses.toLocaleString()}</td><td>{row.profit.toLocaleString()}</td></tr>)}</tbody></table></div></details></>;
}
