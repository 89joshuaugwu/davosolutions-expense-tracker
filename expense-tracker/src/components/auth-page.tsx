import Link from "next/link";
import { ArrowUpRight, Check, ShieldCheck } from "lucide-react";
import { Brand } from "@/components/brand";
import { AuthForm } from "@/components/auth-form";

export function AuthPage({ configured, reset = false, accessDenied = false }: { configured: boolean; reset?: boolean; accessDenied?: boolean }) {
  return <main className="auth-page"><section className="auth-story"><Link href="/login" className="brand-link"><Brand /></Link><div className="auth-story-content"><span className="story-tag"><span /> CLARITY IN EVERY TRANSACTION</span><h1>Know where it goes.<br /><span>See what grows.</span></h1><p>A clearer picture of your company’s spending, revenue, and the numbers that move you forward.</p><div className="story-ledger" aria-hidden="true"><div className="story-ledger-top"><span>YOUR FINANCIAL WORKSPACE</span><span className="story-ledger-dots">•••</span></div><div className="story-bars">{[40, 55, 47, 67, 59, 80, 73, 94, 83, 100].map((height, index) => <div key={index}><i style={{ height: `${height}%` }} /><i style={{ height: `${height * .59}%` }} /></div>)}</div><div className="story-ledger-bottom"><span><i /> Revenue</span><span><i /> Expenses</span><span>Illustration</span></div></div><div className="story-benefits"><span><Check size={15} /> Every expense accounted for</span><span><Check size={15} /> The right access for every role</span></div></div><p className="story-footer">One workspace. A more informed business.</p></section><section className="auth-entry"><div className="auth-mobile-brand"><Brand /></div><div className="auth-entry-inner"><span className="auth-lock"><ShieldCheck size={25} /></span><p className="eyebrow">DAVO SOLUTIONS · INTERNAL WORKSPACE</p><h2>{reset ? "Let’s get you back in." : "Welcome back."}</h2><p className="auth-description">{reset ? "Enter your work email and we’ll send you a password reset link." : "Sign in to manage your expenses with confidence."}</p>{accessDenied && <p className="form-error" role="alert">This account does not have active workspace access. Contact your administrator.</p>}<AuthForm configured={configured} reset={reset} /><div className="preview-entry"><span>Take a look around first</span><Link href="/preview">Explore the design preview <ArrowUpRight size={15} /></Link><small>Fictional sample data · no account needed</small></div></div><div className="auth-entry-footer"><ShieldCheck size={14} /> Private access for the Davo Solutions team</div></section></main>;
}
