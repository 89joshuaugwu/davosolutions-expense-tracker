rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All data flows through authorized Next.js server services. Admin SDK uses IAM.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
