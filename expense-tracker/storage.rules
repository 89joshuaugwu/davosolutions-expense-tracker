rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Attachment provider remains undecided. No direct browser access is permitted.
    match /{path=**} {
      allow read, write: if false;
    }
  }
}
